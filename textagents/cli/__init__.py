"""The two entry points of the experiment.

`run_campaign` runs the nine-call chain, `run_single` runs the one-call control on the same
digests. Both write a raw table, a table combined across replicates, and the units they rejected.
"""
