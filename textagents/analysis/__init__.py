"""The estimation scripts behind the empirical results.

`sample` draws the sampling frame before any reading exists, `har_contrast` runs the nested
in-sample contrasts N0 / N1 / N2, and `oos` runs the out-of-sample Clark-West test and the
concentration checks that go with it.
"""
